{
    "name": "Tab",
    "desc": "Tab for website",
    "code": "new Tab({container: tab})",
    "options": [
        {
            "argument": "container",
            "desc": "ul element of tab list",
            "type": "dom object",
            "default": ""
        }
    ]
}
